// script.js

// You can add JavaScript to handle additional functionalities like
// dynamically loading content, adding animations, or even creating
// interactive elements based on user input.

// Example: Adding a simple alert message on click of an Asana card.

//document.querySelectorAll('.yoga-card').forEach(card => {
  ///  card.addEventListener('click', () => {
     //   alert(`You selected: ${card.querySelector('h3').textContent}`);
    });
});
